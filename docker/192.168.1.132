version: '3.3'
services:
  httpd:
    image: "httpd:${TAG}"
    network_mode: host
  httpd2:
    image: "httpd:${TAG}"
  stat:
    image: trumandk/stat 
    network_mode: host
    volumes:
     - /var/run/docker.sock:/var/run/docker.sock

